function updatesize() {
  window = innerWidth
};

const window = innerWidth
window.addEventListener ("resize", () => {
  if (window <= 450) {
    document.getElementById("mobile").innerHTML.replace= `src="/assets/img/welcome_yolo/welcome-yolo-mobile.png"`
    document.getElementsByClassName
  };
});


updatesize ();
window.addEventListener ("resize", updatesize());

